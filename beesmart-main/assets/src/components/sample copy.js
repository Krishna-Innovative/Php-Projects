import { Component } from '../core/component'
import { get } from '../utilities'

export class sample extends Component {
    constructor(id) {
        super(id)
    }
    init() {
        if (get('')) {
            
        }
    }
}